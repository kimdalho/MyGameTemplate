using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using DG.Tweening;
public class Picker : MonoBehaviour
{
    public int endValue = 3;
    public float duration = 1;

    // Start is called before the first frame update
   /* void Start()
    {
        //transform.DOMove(new Vector3(transform.position.x,transform.position.y + endValue), duration).SetLoops(-1, LoopType.Yoyo);
        transform.DOMoveY(transform.localPosition.y + endValue, 0.2f).SetLoops(-1, LoopType.Yoyo);
    }*/

}
